persistence_path = "/tmp/u3/persistence"
enable_remote_shell = true

[builtin_collectors.device_shadow]
interval_seconds = 1

[tcp_clients.first_one]
port = 8032

[streams.motor_info]
buffer_size = 1024
flush_interval = 60
persistence = { max_file_size = 5000000, max_file_count = 10 }
